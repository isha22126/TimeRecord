<%= form_with(model: teacher) do |form| %>
  <% if teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
        <% teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name,'名前' %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :kana,'ふりがな' %>
    <%= form.text_field :kana %>
  </div>

  <div class="field">
    <%= form.label :mail,'メールアドレス' %>
    <%= form.text_field :mail %>
  </div>

  <div class="field">
    <%= form.label :pw,'パスワード' %>
    <%= form.text_field :pw %>
  </div>

  <div class="field">
    <%= form.label :room_id,'教室' %>
    <%= form.collection_select :room_id, @room, :id, :name %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
